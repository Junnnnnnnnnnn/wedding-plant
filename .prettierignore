# 외부에서 가져온 스킬 파일. 우리 포맷 규칙을 적용하지 않는다.
# (npm run format 이 200여 개 파일을 통째로 건드려 원본과 어긋나게 만든다)
.claude/skills/

# 빌드 산출물
.next/
out/
build/
node_modules/

# 락파일
package-lock.json

# 디자인 시안. 손으로 맞춘 CSS·인라인 SVG 라 포맷터가 건드리면 원본과 어긋난다.
docs/concepts/

# 손으로 맞춘 문서·하네스. 포맷터가 통째로 들쑤신다.
docs/*.html
